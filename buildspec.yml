version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install -g @angular/cli@9.0.6
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - echo "Build phase"
      - ng build --prod
    finally:
      - echo final block echo

artifacts:
  base-directory: codebuild:ap-south-1:121108957987:project/react_project_12
  files:
    - '*/'
  

